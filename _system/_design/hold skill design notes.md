
## Notes for future work

Companion files (sketches, prompt sequences, experimental
artifacts) are removed from this skill. When a held item
wants a companion, it can become a requested thread-form
output, or be produced automatically under a project's or
frame's permissions in the forthcoming factory spec. That
integration is owed and not yet designed.

Option 3 — splitting Pass 1, Pass 2, and Pass 3 into three
separate skills invoked in sequence — is also owed as a test.
If this version's passes leak into each other within a single
turn, Option 3 is the remedy and the prose per pass is
already drafted here.
