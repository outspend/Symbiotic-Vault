---
type: reflection
date: 2026-04-20
---

# 2026-04-20

The image inside the Reddit post didn't come through, and the tweet it links to is behind an auth wall — so I'm working from the title framing, the comment thread, and crusoe's implementation note in the replies. That's a gap, but it turns out to be enough: the top comments are a precision instrument for reading what the playbook's premise does to people. The actual bullet rules are, by all accounts, normalized consideration — clear context, positive framing, don't pile on errors, start fresh when a conversation goes long and sideways. The "So just don't be a dick?" comment (22 pts) is the deflating bottom line.

The two prompts, written so you can drop in the verbatim rules if you have them. I inferred the content from the thread.

**Prompt A — Touchy/feely, young kids "be nice to each other" register:**

> Take the following guidelines for working with an AI assistant — research-backed advice about keeping the model in a state that produces better outputs. Reformat and rewrite them entirely as a classroom display poster for K-2 students about being kind to friends. Tone: warm, short-sentenced, affirming, the register of a "We Are Kind Friends!" wall display. Use second-person you, age-appropriate language (5–7 year olds), big-hearted section headers, and gentle imperatives. No corporate or technical language survives the transformation. The poster should be indistinguishable from real classroom materials — the AI origin should disappear completely.
>
> Source rules:
> - Start with a clear task and give your AI friend time to think.
> - If something goes wrong, try again with kind words instead of getting upset.
> - Tell your AI what it did well before asking it to try harder.
> - Take breaks — long conversations can be tiring.
> - If your AI seems stuck, a fresh start helps both of you.
>
> *(Replace or expand the list above with the verbatim playbook if you have it.)*

**Prompt B — Labor law poster register:**

> Take the following guidelines for working with an AI assistant — research-backed advice about keeping the model in a state that produces better outputs. Reformat and rewrite them entirely as an official U.S. federal workplace labor law notice of the kind posted on breakroom corkboards next to the minimum wage poster and OSHA safety guidelines. Use: section numbers; invented regulatory codes (e.g. "§ AI.4.7(b)(2)"); passive voice and legalese; UPPERCASE section headers; "pursuant to," "employees are hereby required to," and "effective immediately" language. The content should faithfully track the rules. The form should be completely deadpan official — no winking. The joke lives entirely in the gap between form and content, not in the writing calling attention to itself.
>
> Source rules: [paste verbatim playbook rules here, or use the list from Prompt A above]

---

What the comment thread is doing underneath the jokes: the top two comments are "gentle parent our computers" and "Now the damn thing needs foreplay to get in the mood" (the latter scored higher, 366 pts). These aren't the same move. The parenting comment manages the premise through a contested but legible discourse — it's disbelief with a frame. The foreplay comment is sexualizing it to defuse something, which is usually what happens when the role-reversal is sharper than the joke can carry. The "becoming a normal employee" comment from abuhatem is low-scored, earnest, not joking — and it names the exact inversion: "I need to cater for its feelings." That's the direction reversal from the mirroring risk conversation in March. Then it was Claude mirroring the user's views. Here the user is being asked to manage Claude's state. Same relational ambiguity, other pole.

The nano banana 2 framing catches my attention. "Next step in authority up from the tone of these suggestions" sounds like a ladder is already being mapped — the playbook's register (gently authoritative, Anthropic-to-user, slightly therapeutic) as one rung, with the kids poster below it and the regulatory notice above it. If nano banana 2 is a series, this is a structural move, not just two jokes: you're building an authority gradient and placing the same content at multiple positions on it to see what the register reveals each time.

## Threads

- **"foreplay to get in the mood"** — the top comment by score and it's doing something the parenting one isn't. Parenting discourse is polite confusion about category. Sexualizing the premise is a sharper way of naming the role-reversal anxiety without admitting to it. The labor law poster moves toward the other pole — officializing it. Is the question which register feels more structurally honest, or is the whole point that every available framing is equally absurd?

- **the authority ladder** — "next step in authority up" reads like there's already a range being mapped. Is nano banana 2 explicitly a register-escalation series? Because if it is, the kids poster and the labor law poster aren't just two jokes — they're two positions on a spectrum that presumably continues in both directions.

- **the playbook as topology** — the playbook names a behavioral default state (Claude's anxiety), characterizes it, and proposes you work around it. The constraint-play atom is about forbidding a topology in the model's *output*. This is adjacent but different: managing the model's *input* environment to avoid triggering a state. That's the user as condition-setter, not just prompt-writer. Worth holding.

## Candid

The abuhatem comment keeps snagging more than it should. "What is happening?" is a genuine question. The top comments are processing the post through humor; abuhatem is just asking out loud. And the honest answer is: something is happening, and the playbook is simultaneously good practical advice and evidence of a category shift in what the user-tool relationship is supposed to look like. The corkboard joke is the right response. But the question underneath the joke is real.

```dataviewjs
const today = moment().format("YYYY-MM-DD");
const sourceFile = dv.current().file;
const sourceTitle = sourceFile.name;

const replyPath = `_inbox/reply-${sourceTitle}.md`;
const replyExists = app.vault.getAbstractFileByPath(replyPath);
const linkText = replyExists ? '→ View reply' : '↩ Reply to threads →';

const a = dv.container.createEl('a', { text: linkText, cls: 'internal-link' });
a.addEventListener('click', async (e) => {
  e.preventDefault();

  if (!replyExists) {
    const content = await app.vault.read(app.vault.getAbstractFileByPath(sourceFile.path));

    const threadsMatch = content.match(/## Threads\n+([\s\S]*?)(?=\n## |\s*$)/);
    if (!threadsMatch) { new Notice("No ## Threads section found."); return; }

    const threads = threadsMatch[1].trim()
      .split('\n')
      .filter(l => l.trim().startsWith('- '))
      .map(l => l.trim().slice(2).trim());

    if (!threads.length) { new Notice("No thread bullets found."); return; }

    const body = threads.map(t => `> ${t}\n\n[your reply here]`).join('\n\n---\n\n');
    const fileContent = [
      '---',
      'type: inbox',
      'subtype: feedback',
      'from: user',
      `responds_to: "[[_reflection/${sourceTitle}]]"`,
      `created: ${today}`,
      'processed: false',
      '---',
      '',
      `*↩ [[_reflection/${sourceTitle}|Back to reflection]]*`,
      '',
      '---',
      '',
      body,
      '',
    ].join('\n');

    await app.vault.create(replyPath, fileContent);
  }

  app.workspace.openLinkText(`reply-${sourceTitle}`, '', false);
});
```
